const fs = require('fs'); const content = fs.readFileSync('centro-dental-aa-frontend-main/src/components/PacienteList.tsx', 'utf-8'); const lines = content.split('\n'); const start = lines.findIndex(l => l.includes('const handlePrintPaciente =')); const end = lines.findIndex((l, i) => i > start && l.includes('return (') && lines[i-1].includes('};')); const funcBody = lines.slice(start, end).join('\n'); const outContent = \import Swal from 'sweetalert2';\nimport api from '../services/api';\nimport { Paciente } from '../types';\nimport { formatDate, calcularEdad } from './dateUtils';\nimport { formatFullName } from './formatters';\n\nexport \\; fs.writeFileSync('centro-dental-aa-frontend-main/src/utils/printUtils.ts', outContent); console.log('Done!');
